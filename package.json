{
  "name": "wso2is-repo-explorer-root",
  "version": "1.0.0",
  "description": "Root package.json to manage backend and frontend for Glitch deployment.",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
